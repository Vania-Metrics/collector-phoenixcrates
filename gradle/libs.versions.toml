# COMPILE-ONLY dependencies — no third-party jar is bundled in the produced jar.
# The VaniaMetrics API is not here: it comes from the core git repo (see
# gradle.properties and settings.gradle.kts).
#
# Third-party plugins come from the Modrinth Maven repo, at the exact version
# id: it's the same file, byte for byte, as the download link.
#
# SHA-256 checksums of everything resolved are pinned in
# gradle/verification-metadata.xml. After a version bump:
#   ./gradlew --write-verification-metadata sha256 build
#
# paper-api is a dated snapshot: Paper does not publish a fixed version of its
# API, the timestamp is the version. It's read as a fixed version (see
# settings.gradle.kts), so WITHOUT its transitive dependencies: the "paper"
# bundle declares the ones compilation needs, at the versions from paper-api's
# .pom. PhoenixCrates Lite — classes at 52 (Java 8), so readable by
# javac --release 21: unlike ExcellentEconomy, the API compiles directly. Only
# the internal class managers.players.PlayerData goes through reflection, and
# that's a deliberate choice.
[versions]
paper-api = "1.21.11-R0.1-20260511.115010-91"
adventure = "4.26.1"

[libraries]
paper-api = { module = "io.papermc.paper:paper-api", version.ref = "paper-api" }
adventure-api = { module = "net.kyori:adventure-api", version.ref = "adventure" }
adventure-key = { module = "net.kyori:adventure-key", version.ref = "adventure" }
examination-api = { module = "net.kyori:examination-api", version = "1.3.0" }
jspecify = { module = "org.jspecify:jspecify", version = "1.0.0" }
checker-qual = { module = "org.checkerframework:checker-qual", version = "3.49.2" }
jetbrains-annotations = { module = "org.jetbrains:annotations", version = "24.1.0" }
guava = { module = "com.google.guava:guava", version = "33.3.1-jre" }
# PhoenixCratesLite 6.2.1 — maven.modrinth:<projet>:<id de version Modrinth>
phoenixcrates = { module = "maven.modrinth:phoenix-crates-lite", version = "ks24Yz4t" }

[bundles]
paper = ["paper-api", "adventure-api", "adventure-key", "examination-api", "jspecify", "checker-qual", "jetbrains-annotations", "guava"]
